TASK: Auto-generate a secure SIGNING_SECRET, fix the wiring, and show me the exact value to paste into Railway + Cloudflare.

Context:
- This project signs HLS URLs / segment access using HMAC.
- Currently SIGNING_SECRET is NOT set and code falls back to SESSION_SECRET or a hardcoded default (unsafe).
- I have zero coding knowledge. Do everything in code.

GOALS (must complete all):
1) Generate a cryptographically secure random SIGNING_SECRET (64 chars or 32 bytes base64/hex) at setup time.
2) Remove ALL fallbacks (no SESSION_SECRET fallback, no hardcoded default).
3) Make SIGNING_SECRET REQUIRED for production. If missing, fail fast with a clear error:
   "SIGNING_SECRET env var is required. Generate one and set it in Railway + Cloudflare Worker."
4) Add a helper endpoint ONLY for ADMIN (protected) to generate/rotate signing secret:
   - Route: GET /api/admin/generate-signing-secret
   - Access: admin-only (same auth as existing admin panel)
   - Response JSON: { signing_secret: "<NEW_SECRET>" }
   - This endpoint must NOT store the secret automatically (because Railway/Cloudflare env are manual). Just display it.
5) Add a startup log (never print the secret):
   console.log("[security] SIGNING_SECRET loaded:", !!process.env.SIGNING_SECRET);
6) Ensure ALL signing/verification functions for:
   - HLS sid/st/exp tokens
   - embed/share link tokens (if they use signing)
   use ONLY process.env.SIGNING_SECRET (no other secret).
7) Update any docs/readme or a clear comment in code telling me:
   - Put SAME SIGNING_SECRET in Railway variables AND Cloudflare Worker variables.
   - Then redeploy both.

DELIVERABLES:
A) Tell me which files you changed.
B) Give me the generated SIGNING_SECRET value from /api/admin/generate-signing-secret.
C) Confirm there are NO hardcoded secrets and NO fallback to SESSION_SECRET.

IMPORTANT RULES:
- Do NOT break existing playback.
- Keep current HMAC algorithm the same (HMAC-SHA256).
- Never expose the secret in logs.
- Endpoint must require admin auth.
- If admin auth is not implemented for API routes, implement it using existing admin session/JWT logic used in the app.